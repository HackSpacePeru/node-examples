Si deseas ejecutar el siguiente proyecto, primero hay que instalar las dependecias y modulos:

```bash
$ npm install
```

Para ejecutar la aplicacion:

```bash
node server.js agregar -p="Peru" -c="Lima"

```
